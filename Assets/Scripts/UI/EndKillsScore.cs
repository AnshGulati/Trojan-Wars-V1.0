using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class EndKillsScore : MonoBehaviour
{
    //public GameObject KillScore;
    /*private TMPro.TextMeshPro killScoreText;
    private ScoreUI scoreUI;

    private void Awake()
    {
        killScoreText = GetComponent<TextMeshPro>();
        killScoreText.text = $"TOTAL KILLS: {scoreUI.scoreText}";

        //killScoreText.text = PlayerPrefs.GetString("KillScore");
    }*/
}
